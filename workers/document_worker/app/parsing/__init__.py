"""Generic document parsing (M3): Docling integration, page classification,
parser cascade, region segmentation, and generic tree building.

Only the GENERIC layer is implemented here (addendum §2: LAYOUT -> GENERIC
STRUCTURE -> STRUCTURAL REGION CLASSIFICATION). Specialized interpretation
(Pasal/Ayat/Huruf, semantic roles) is M4, implemented in apps/api/app/parsing.
"""
