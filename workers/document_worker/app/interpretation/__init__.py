"""M4 specialized interpretation (addendum §2's SPECIALIZED INTERPRETATION ->
CANONICAL HIERARCHICAL TREE stage). Operates on M3's already-persisted
generic regions/nodes — it re-types and annotates, it never builds a tree
from scratch (that's tree_builder.py, M3).
"""
