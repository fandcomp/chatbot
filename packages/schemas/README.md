# Schemas

Shared contract types between `apps/api` and `apps/web` — OpenAPI-generated TypeScript types from the FastAPI backend's schema, plus any shared structured-output contracts (e.g. the LLM structured answer shape).

Populated starting **M9 — LLM Integration**, once `LLMGateway`/`EmbeddingGateway`/`RerankerGateway` interfaces stabilize.

See `docs/MASTER_DEVELOPMENT_SPEC.md` §35-38.
