{
  "name": "e2e",
  "private": true,
  "version": "0.1.0",
  "description": "Cross-app Playwright e2e — see ../README.md and seed.py.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1"
  }
}
