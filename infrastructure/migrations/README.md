# Infrastructure / Migrations

Reserved for non-Alembic infrastructure setup/migration scripts (e.g. Qdrant collection bootstrap, MinIO bucket policy setup).

Application database migrations (Alembic) live in `apps/api/alembic/`, not here.

Populated as needed.
