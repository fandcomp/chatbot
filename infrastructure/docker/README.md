# Infrastructure / Docker

Additional Dockerfiles (worker image, production API image) beyond the infra-only services defined in the root `docker-compose.yml` (Postgres, Redis, Qdrant, MinIO).

Populated in **M15 — Production Hardening**, or earlier if a service needs its own image before then.

See `docs/MASTER_DEVELOPMENT_SPEC.md` §55, §98 (M15).
